/*
Point構造体を作成し、以下を実装しなさい。
メンバー変数 x と y（int型）を持つこと。
operator+ をオーバーロードして、2つのPointインスタンスを足し合わせて新しいPointを返す。
operator<< をオーバーロードして、std::cout << Point で (x, y) の形式で出力できるようにする。
operator+= をオーバーロードして、this を返す形で自身の値を更新できるようにする。
メンバー関数 setXY(int x, int y) を作成し、this ポインタを使ってメンバー変数を更新する。
 */

#include <iostream>


st

int main()
{
    
    
    
   
    return 0;
}
